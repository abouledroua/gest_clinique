import 'package:flutter/material.dart';

class AuthSectionWidget extends StatelessWidget {
  const AuthSectionWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
